@model IdentitySample.Models.Emprestimo

@{
    ViewBag.Title = "Datalhes";
}

<h2>Datalhes</h2>
@using (Html.BeginForm("Devolver", null, new { id = Model.Id }, FormMethod.Post))

{
    @Html.AntiForgeryToken()
    <div>
        <h4>Empréstimo</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Livro.Titulo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Livro.Titulo)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DataEmprestimo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DataEmprestimo)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DataPrevisaoDevolucao)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DataPrevisaoDevolucao)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Status)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Status)
            </dd>

        </dl>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Devolver" class="btn btn-success" />
                
            </div>
        </div>
    </div>
}
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Voltar", "Index")
</p>